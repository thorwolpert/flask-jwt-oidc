name: Publish to PyPI

on:
  push:
    tags:
      - 'v*' # Triggers only when you push a tag like v0.9.0
  workflow_dispatch: # Allows manual triggering from the UI
    inputs:
      version_override:
        description: 'Optional: Version to publish (if not using tag)'
        required: false
        default: ''

jobs:
  build-n-publish:
    name: Build and publish Python 🐍 distribution 📦 to PyPI
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        run: pipx install poetry

      - name: Build package
        run: poetry build

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
